executez le make : aller dans le répertoire "APIGroupe", puis faire la commande "make" dans un 
                    terminal.

executez la doc : aller dans le répertoire "APIGroupe", puis faire la commande "make doc" dans un 
                    terminal. 
